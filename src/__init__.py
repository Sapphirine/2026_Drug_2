# Intentionally kept empty so that `from src.colab_init import setup`
# works before requirements.txt is installed.
# Import submodules explicitly where needed, e.g.:
#     from src.evaluation import MolEvaluator
#     from src.docking import VinaDocker
